Type: "AWS::EC2::NetworkAclEntry"
Properties: 
  CidrBlock: String # Conditional. You must specify the CidrBlock or Ipv6CidrBlock property.
  Egress: Boolean # By default, AWS CloudFormation specifies false.
  Icmp: # Conditional. Required if specifying 1 (ICMP) for the protocol parameter.
    EC2 ICMP
  Ipv6CidrBlock: String # Conditional. You must specify the CidrBlock or Ipv6CidrBlock property.
  NetworkAclId: String # REQUIRED
  PortRange: # Conditional Required if specifying 6 (TCP) or 17 (UDP) for the protocol parameter.
    EC2 PortRange
  Protocol: Integer # REQUIRED
  RuleAction : String # REQUIRED
  RuleNumber : Integer # REQUIRED

# NetworkAclEntry ICMP
Code: Integer # Conditional. Required if you specify 1 (ICMP) for the CreateNetworkAclEntry protocol parameter.
Type: Integer # Conditional. Required if you specify 1 (ICMP) for the CreateNetworkAclEntry protocol parameter.

# NetworkAclEntry PortRange
From: Integer # Conditional. Required if you specify 6 (TCP) or 17 (UDP) for the protocol parameter.
To: Integer # Conditional. Required if you specify 6 (TCP) or 17 (UDP) for the protocol parameter.

Return Values:
  Ref: When the logical ID of this resource is provided to the Ref intrinsic function, Ref returns the resource name.